#include <stdio.h>
#include <stdlib.h>

int findDigits(int n)
{
    if (n == 0)
        return 0;

    return 1 + findDigits(n/10);
}


int main()
{
    int n;
    scanf("%d", &n);

    int res = findDigits(n);
    printf("%d", res);

    return 0;
}
