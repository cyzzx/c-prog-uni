#include <stdio.h>
#include <stdlib.h>

int sumOfDigits(int n)
{
    int sum = 0;
    if (n / 10 == 0)
        return n;

    else
        return (n % 10) + sumOfDigits(n / 10);

}

int main()
{
    int n;
    scanf("%d", &n);

    int res = sumOfDigits(n);
    printf("%d", res);

    return 0;
}
