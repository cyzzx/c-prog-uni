#include <stdio.h>
#include <stdlib.h>

int power(int n, int e)
{
    if(e == 0)
    {
        return 1;
    }

    return n * power(n, e-1);
}


int main()
{
    int n, x;
    printf("Enter the numbers :");
    scanf("%d %d", &n, &x);

    int result = power(n, x);
    printf("%d", result);
}
