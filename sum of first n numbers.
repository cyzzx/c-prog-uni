#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int sum(int n)
{
    if (n == 0)
    {
        return 0;
    }

    return n + sum(n-1);
}

int main()
{
    int n;
    scanf("%d", &n);

    int total = sum(n);
    printf("%d", total);
}
