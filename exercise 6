#include <stdio.h>
#include <stdlib.h>

int seqChange(int *addrB)
{

    if(*addrB == 35)
        *addrB = (*addrB) * 2;

    else
        *addrB = (*addrB) * 5;

    return *(addrB + 1) + 
}


int main()
{
    


    return 0;
}
